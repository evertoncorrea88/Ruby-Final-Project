<div class="row row-offcanvas row-offcanvas-right">

    <div class="col-xs-12 col-sm-9">
        <p class="pull-right visible-xs">
            <button type="button" class="btn btn-primary btn-xs" data-toggle="offcanvas">Toggle nav</button>
        </p>
            
        <div class="jumbotron">
            <h3>New Guest</h3><br>
            
            <%= form_for(@guest) do |f| %>
            <% if @guest.errors.any? %>
            <div id="error_explanation">
            <h2><%= pluralize(@guest.errors.count, "error") %> prohibited this guest from being saved:</h2>

            <ul>
            <% @guest.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
            </ul>
            </div>
            <% end %>

            <div class="field">
            <%= f.label :name %><br>
            <%= f.text_field :name %>
            </div>
            <div class="field">
            <%= f.label :email %><br>
            <%= f.text_field :email %>
            </div>
            <div class="field">
            <%= f.label :phone %><br>
            <%= f.text_field :phone %>
            </div>
            <div class="actions">
            <%= f.submit "Create Guest", class: "btn btn-primary" %>
            </div>
            <% end %>
        </div>

    </div>


    <div class="col-xs-6 col-sm-2 sidebar-offcanvas" id="sidebar" role="navigation">
        <div class="list-group">
            <%= link_to 'Home Page', home_page_index_path,  class: "list-group-item" %>
            <%= link_to 'New Guest', new_guest_path,  class: "list-group-item active" %>
            <%= link_to 'New Check in', register_index_path, class: "list-group-item" %>
            <%= link_to 'Reports', register_list_check_in_path, class: "list-group-item" %>
            <%= link_to 'Logout', home_page_login_path, class: "list-group-item" %>
        </div>
    </div><!--/.sidebar-offcanvas-->
</div>